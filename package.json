{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "lerna run start --parallel"
  },
  "devDependencies": {
    "lerna": "^8.0.1"
  },
  "workspaces": [
    "packages/local-client",
    "packages/cli",
    "packages/local-api"
  ]
}
